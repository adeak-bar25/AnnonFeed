<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content="MyWebSite" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join | AnnonFeed</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=info" />
</head>

<body class="white-bg">
    {{{nav}}}
    <div class="main px-6 md:flex-col-center">
        <div class="max-md:mt-4 md:w-fit mx-auto rounded-md md:pl-[1.95rem] md:pr-[2.1rem] md:pt-7 md:bg-gray-800">
            <div class="md:w-fit md:mx-auto">
                <h1 class="text-6xl font-bold md:text-center">Masuk<br class="md:hidden"> Ke Event</h1>
                <p class="text-lg mt-1 text-slate-200 md:text-center">*Untuk Peserta</p>
            </div>
            <div>
                <form id="sign-up" method="post" action="/join">
                    {{{error}}}
                    <p class="font-medium text-lg mt-2 md:mt-5 mb-3 md:text-center">Masukkan Kode 6 Digit Yang Diberikan oleh Panitia
                    </p>
                    <label for="code" class="my-5 pt-2 pb-4 block auto w-full">
                        <input type="number"
                            class="bg-transparent border-white border-2 focus:outline-none block w-full md:w-[35%] mx-auto text-[40px] text-center rounded-md font-bold tracking-[.3rem] py-2 md:my-5 min-w-56"
                            id="code" name="code" max="999999" placeholder="______">
                        <button disabled type="submit" id="button-code"
                            class="btn-square w-full mt-5 md:mt-7 md:mx-auto">Lanjutkan</button>
                    </label>
                </form>
            </div>
        </div>
    </div>
    <script>

        const code = document.getElementById('code')
        const button = document.querySelector('#button-code')
        code.addEventListener('input', (e) => {
            if (code.value.length < 6 || code.value.length === 0) {
                button.disabled = true;
                if (code.value.length !== 0) code.style.borderColor = '#cf0909';
            } else if (code.value.length > 6) {
                code.value = code.value.slice(0, 6);
            }
            else {
                button.disabled = false;
                code.style.borderColor = '#03c203';
            }
        })
    </script>
    <script src="script.js"></script>
</body>

</html>