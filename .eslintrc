{
    "rules": {
        "indent": ["error", 4],
        "no-unused-vars": "warn",
        "no-unused-imports": "warn",
        "rules": {
                "semi": ["error", "always"]
            }
    }
}
